# ============================================================================
# AUVIA CONFIGURATION
# ============================================================================
# Copy this file to .env and customize for your setup
# cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ----------------------------------------------------------------------------
POSTGRES_USER=auvia
POSTGRES_PASSWORD=change_me_in_production    # ⚠️ CHANGE THIS!
POSTGRES_DB=auvia

# ----------------------------------------------------------------------------
# SECURITY
# ----------------------------------------------------------------------------
# Generate with: openssl rand -hex 32
SECRET_KEY=change-me-in-production-use-a-long-random-string    # ⚠️ CHANGE THIS!

# ----------------------------------------------------------------------------
# PORT CONFIGURATION
# ----------------------------------------------------------------------------
FRONTEND_PORT=3000    # Web interface port
BACKEND_PORT=8001     # API server port

# ----------------------------------------------------------------------------
# MUSIC STORAGE PATHS
# ----------------------------------------------------------------------------
# Configure paths to your music storage locations
# These paths are mounted into the Docker container
# You can use 1-3 storage locations

MUSIC_PATH_1=/path/to/your/music           # Primary storage (required)
# MUSIC_PATH_2=/path/to/your/music2        # Secondary storage (optional)
# MUSIC_PATH_3=/path/to/your/music3        # Tertiary storage (optional)

# ----------------------------------------------------------------------------
# FRONTEND API URL (Optional)
# ----------------------------------------------------------------------------
# Leave commented out for automatic detection (recommended)
# Only set this if you need to override the auto-detected URL
# VITE_API_URL=http://your-server-ip:8001

# ============================================================================
# QOBUZ CREDENTIALS
# ============================================================================
# Qobuz credentials are configured through the web interface Settings page
# Do NOT put them in this file for security reasons
# ============================================================================
